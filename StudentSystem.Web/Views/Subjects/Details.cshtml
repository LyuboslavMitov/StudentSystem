@model StudentSystem.Web.Models.SubjectViewModel

@{
    ViewBag.Title = "Details";
}
@Html.Partial("_sidebar")
@Html.Partial("_helpbutton")
<body class="theme-invert">
    <div class="jumbotron col-md-10 col-lg-10 col-md-offset-1 col-lg-offset-1 text-center" style="color: black">
        <div class="page-header">
            <h1>Предмети</h1>
        </div>

        <div>

            <hr />
            <dl class="dl-horizontal">
                <dt>
                    @Html.DisplayNameFor(model => model.SubjectName)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.SubjectName)
                </dd>

            </dl>
        </div>
        <p>
            @Html.ActionLink("Edit", "Edit", new { id = Model.SubjectID }) |
            @Html.ActionLink("Back to List", "Index")
        </p>
    </div>
</body>